import SwiftUI

/// App-specific theme tokens (wraps `LightTheme` / `DarkTheme`).
struct AppTheme {
    let brand: Color
    let neutral: Color
    let background: Color
    let surface: Color
    let text: Color
    let textSecondary: Color
    let accent: Color
    let destructive: Color
}

